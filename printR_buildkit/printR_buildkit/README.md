# BuidLer PrintR — Master Copies (Build Kit)

This zip contains:
- Figma build instructions + frame specs
- UI kit specs + design tokens
- React + Telegram mini-app starter (state machine + sound + haptics)

Start here:
1) `figma/README.md`
2) `ui-kit/components.md`
3) `frontend/COMPONENT_MAP.md`

Update: Full screen set added (Progress/Store/Governance/Profile) + NavBar routing.

Update: Added Prize Vault, Leaderboard, Round History screens + VRF adapter interface + pending settlement banner.

Update: Added Solana on-chain VRF adapter scaffold + runtime switch (SIM vs ONCHAIN) in useGameMachine.

