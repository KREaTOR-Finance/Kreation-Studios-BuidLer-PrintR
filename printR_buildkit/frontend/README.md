# PrintR Frontend Starter (Telegram Mini App)

This is a lightweight starter scaffold + core logic hooks.
To run locally you would normally:
- `npm i`
- `npm run dev`

(Dependencies are not included in this zip; this is the blueprint + code.)
